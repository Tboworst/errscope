services:
  beacon:
    build: .
    ports:
      - "7000:7000"
    volumes:
      # persist the database outside the container
      - ./beacon.db:/app/beacon.db
    env_file:
      - .env
    restart: unless-stopped
