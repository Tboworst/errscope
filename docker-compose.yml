services:
  errscope:
    build: .
    ports:
      - "7000:7000"
    volumes:
      # persist the database outside the container
      - ./errscope.db:/app/errscope.db
    env_file:
      - .env
    restart: unless-stopped
